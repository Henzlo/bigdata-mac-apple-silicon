# ── Docker volumes & local data ──────────────────────────
**/data/
**/logs/
**/tmp/

# ── Environment overrides (keep hadoop.env, ignore personal overrides) ──
.env.local
.env.override
*.local.env

# ── macOS junk ───────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# ── IDE files ────────────────────────────────────────────
.vscode/
.idea/
*.iml

# ── Python (if you add notebooks or scripts) ─────────────
__pycache__/
*.py[cod]
*.pyo
.ipynb_checkpoints/

# ── Secrets (never commit these) ─────────────────────────
secrets/
*.pem
*.key
*.crt
